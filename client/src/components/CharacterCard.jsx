import { esperColors } from '../logic/esperEngine';
export function CharacterCard({ c, onClick }) { return <button onClick={onClick} draggable onDragStart={(e)=>e.dataTransfer.setData('text/plain', c.id)} className="glass rounded-xl p-3 text-left hover:scale-[1.02] transition shadow-[0_0_22px_rgba(34,211,238,0.1)]"><img src={c.image} className="h-36 w-full object-cover rounded-lg"/><div className="mt-2 font-semibold">{c.name}</div><div className="text-xs text-zinc-400">{c.role}</div><span className={`inline-block mt-2 px-2 py-1 text-xs border rounded ${esperColors[c.esper]}`}>{c.esper}</span></button>; }
