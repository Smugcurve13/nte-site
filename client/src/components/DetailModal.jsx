import { esperColors } from '../logic/esperEngine';
export function DetailModal({ character, synergies, onClose }) { if (!character) return null; return <div className="fixed inset-0 bg-black/70 flex items-center justify-center p-4" onClick={onClose}><div className="glass max-w-2xl w-full rounded-2xl p-5" onClick={(e)=>e.stopPropagation()}><img src={character.image} className="w-full h-56 object-cover rounded-xl"/><h2 className="text-2xl mt-3">{character.name}</h2><div className="flex gap-2 mt-2"><span>{character.role}</span><span className={`px-2 border rounded ${esperColors[character.esper]}`}>{character.esper}</span></div><p className="text-zinc-300 mt-3">{character.description}</p><h3 className="mt-4 font-semibold">Best Synergies</h3><ul className="grid grid-cols-2 gap-2 mt-2">{synergies.map(s=><li key={s.id} className="glass rounded p-2">{s.name} · {s.esper}</li>)}</ul></div></div>; }
